name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - toolz>=0.8.2
    - dask
    - matplotlib
    - numpy
    - pandas
    - scipy
    - sklearn
    - pykrige
  #  - codecov

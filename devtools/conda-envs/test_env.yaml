name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - dask
    - matplotlib
    - numpy
    - pandas
    - scipy
    - sklearn
    - pykrige
  #  - codecov

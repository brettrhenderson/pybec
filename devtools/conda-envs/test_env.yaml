name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - dask
    - jupyter
    - matplotlib
    - numpy
    - pandas
    - Pillow
    - scikit-image
    - scikit-learn
    - scipy
    - sklearn
    - pykrige
  #  - codecov
